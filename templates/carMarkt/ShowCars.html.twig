{% extends 'base.html.twig' %}

{% block body %}
<div style="margin-top: 12vh; text-align: center;" class="cardsss" ><h1>Our Cars</h1></div>
<div class="container" style="margin-top: 4vh;" >
<div class ="row">
    
{% for car in cars %}
      <div class="card col-sm-3  " style="width: 26rem; margin: 8px; cursor: pointer;"   >
    <div class="card-body" onclick= "location.href='/Cars/{{ car.id }}'">
          <img class="card-img-top " src= ' {{ car.mainImage }}' alt="Card image cap" style="width: 100%;">
          <h5 class="card-title">{{ car.productName }}</h5>
          <p class="card-text">{{ car.productDescription }}</p>
          <p class="card-text">£{{ car.price }}</p>
      </div>
       <form action="classes/session.php" method="post">
      <input type="hidden" name="product_id" value="{{ car.id }}">
      <button type="submit" class="add btn btn-primary bluebtn mb-4" style="width: 100%"  >Add to Basket</button>
      </form> 
      </div>
  
    
{% endfor %}

</div>
</div>   
{% endblock %}